const prompt = require('prompt-sync')({ sigint: true }); // Import prompt-sync for user input

const numbers=[]
numbers[0] = (prompt("Enter the first number: "));
numbers[1] = (prompt("Enter the second number: "));
numbers[2] = (prompt("Enter the third number: "));
numbers[3] = (prompt("Enter the fourth number: "));
numbers[4] = (prompt("Enter the fifth number: "));
numbers[5] = (prompt("Enterg the sixth number: "));

// was not sure which math to use to get the prime numbers out of the array

console.log(numbers)